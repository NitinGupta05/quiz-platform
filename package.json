{
  "name": "quiz-platform",
  "version": "1.0.0",
  "description": "Quiz Platform - MERN Stack Application",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm start",
    "dev": "echo 'Run client and server separately'",
    "install:all": "cd client && npm install && cd ../server && npm install"
  }
}

